#+TITLE: Emacs Configuration
* Emacs configuration

Here is an org-babel-tangle to deploy init.el and early-ini.el under ~$HOME/.emacs.d~ directory.

#+begin_src elisp :results none
  ;; local variables
  (setq-local config-files (list "init.el" "early-init.el"))
  (setq-local dst-dir (expand-file-name "~/.emacs.d"))
  (setq-local tmp-dir (file-name-base buffer-file-name))
  (setq-local backup-prefix (format-time-string "%Y-%m-%d_%H%M%S_"))
  ;; Create directories
  (dolist (dir-name (list dst-dir tmp-dir (file-name-concat dst-dir "file-backup")))
    (make-directory dir-name t))
  ;; Generate file
  (org-babel-tangle-file buffer-file-name)
  ;; Copy current files and deploy
  (dolist (config-file config-files)
    (let ((tmp-file (file-name-concat tmp-dir config-file))
          (bak-file (file-name-concat tmp-dir (concat backup-prefix config-file)))
          (dst-file (file-name-concat dst-dir config-file)))
      ;; Take backup
      (if (file-exists-p dst-file)
          (copy-file dst-file bak-file)
        nil)
      ;; Save new file
      (copy-file tmp-file dst-file t)))
#+end_src


** Requirements
- Git (required by straight.el)

* Early init file - ~$HOME/.emacs.d/early-init.el~

https://www.gnu.org/software/emacs/manual/html_node/emacs/Early-Init-File.html
#+begin_src elisp :tangle emacs/early-init.el :mkdirp yes
  ;; -*- coding: utf-8; lexical-binding: t; -*-
  ;; early-init.el - Emacs configuration file
  ;; Generated by emacs.org file using org-babel-tangle-file
#+end_src

** Disable packages
Disable ~packages~ to make sure to use ~straight.el~ for package management.

#+begin_src elisp :tangle emacs/early-init.el
  (setq package-enable-at-startup nil)
#+end_src

* Init file - ~$HOME/.emacs.d/init.el~
#+begin_src elisp :tangle emacs/init.el
  ;; -*- coding: utf-8; lexical-binding: t; -*-
  ;; init.el - Emacs configuration file
  ;; Generated by emacs.org file using org-babel-tangle-file
#+end_src

#+RESULTS:

** straight.el
https://github.com/radian-software/straight.el

#+begin_src elisp :tangle emacs/init.el
  ;; Straight.el
  (defvar bootstrap-version)
  (let ((bootstrap-file
         (expand-file-name
          "straight/repos/straight.el/bootstrap.el"
          (or (bound-and-true-p straight-base-dir)
              user-emacs-directory)))
        (bootstrap-version 7))
    (unless (file-exists-p bootstrap-file)
      (with-current-buffer
          (url-retrieve-synchronously
           "https://raw.githubusercontent.com/radian-software/straight.el/develop/install.el"
           'silent 'inhibit-cookies)
        (goto-char (point-max))
        (eval-print-last-sexp)))
    (load bootstrap-file nil 'nomessage))
#+end_src

#+RESULTS:
: t

** straight-use-package

https://github.com/jwiegley/use-package

#+begin_src elisp :tangle emacs/init.el
  ;; straight-use-package
  (straight-use-package 'use-package)
#+end_src

** Encoding

Use utf-8 as a default encoding.
#+begin_src elisp :tangle emacs/init.el
  ;; Encoding
  (setq default-file-name-coding-system 'utf-8)
  (setq default-buffer-file-coding-system 'utf-8)
  (setq default-process-coding-system '(utf-8 . utf-8))
#+end_src

** xclip

Synchronize Clipboard with Copy & Paste
https://elpa.gnu.org/packages/xclip.html

It works in multiple environments.
#+begin_quote
- Under X11: `xclip' or `xsel' (https://xclip.sourceforge.net and
  https://www.vergenet.net/~conrad/software/xsel/ respectively).
- MacOS: `pbpaste/pbcopy'
- Cygwin: `getclip/putclip'
- Under Wayland: `wl-clipboard' (https://github.com/bugaevc/wl-clipboard)
- Termux: `termux-clipboard-get/set'
- Emacs: It can also use Emacs's built-in GUI support to talk to the GUI.
#+end_quote

#+begin_src elisp :tangle emacs/init.el
  ;; Synchronize clipboard
  (use-package xclip
    :straight t
    :config
    (setq xclip-mode 1))
#+end_src

** Alias

| Alias       | Original function       |
|-------------+-------------------------|
| kill-daemon | sava-buffers-kill-emacs |

#+begin_src elisp :tangle emacs/init.el
  ;; Alias
  (defun kill-daemon ()
    "Alias of sava-buffers-kill-emacs"
    (interactive)
    (save-buffers-kill-emacs))
#+end_src

** Misc

#+begin_src elisp :tangle emacs/init.el
  ;; Misc
  (setq inhibit-startup-message t)       ;; Hide Startup Gnus
  (setq visible-bell t)                  ;; Visible bell (t: Enable, nil: Disable)
  (menu-bar-mode -1)                     ;; Menu bar (1: Enable, -1: Disable)
  (tool-bar-mode -1)                     ;; Tool bar (1: Enable, 0 or negative: Disable)
  (blink-cursor-mode 0)                  ;; Blink Cursor (0: off, 1: blink)
  (show-paren-mode 1)                    ;; Emphasize pair parentheses
  (setq dired-listing-switches "-alhv")  ;; dired human readable
  (global-unset-key (kbd "C-z"))         ;; disable minimize window on macOS GUI mode. The key also assigned to the default tmux key in terminal.
  (setq delete-by-moving-to-trash t)     ;; Move deleted files to trash

  ;; Display cursor position
  (line-number-mode t)                   ;; Line number
  (column-number-mode t)                 ;; Column number
  ;; Mode line
  (display-time-mode 1)                  ;; Display time
  (setq display-time-day-and-date t)     ;; Display date
  (which-function-mode 1)                ;; Display function name
  ;; File, Buffer
  (setq auto-save-default nil)           ;; No auto-save
  (global-auto-revert-mode 1)            ;; Reload modified buffer automatically
  ;; Tab, Indent
  (setq-default tab-width 4)             ;; Tab width: 4 space
  (setq-default indent-tabs-mode nil)    ;; No tab
  (electric-indent-mode 0)               ;; Prevent auto-indent when pasting
  ;; Font lock
  ;; (setq font-lock-global-modes t)
  (setq text-mode-hook 'turn-off-auto-fill)  ;; Turn off auto-fill

  (global-set-key (kbd "M-m") 'kmacro-call-macro)  ;; call kmacro

#+end_src

** GUI Config

Font: HackGen Console NF
https://github.com/yuru7/HackGen

#+begin_src elisp :tangle emacs/init.el
  (defun setup-fonts ()
    (let ((font-name "HackGen Console NF"))
      ;; 全環境共通：デフォルト・固定幅フォントの設定
      (set-face-attribute 'default nil :family font-name :height 140)
      (set-face-attribute 'fixed-pitch nil :family font-name :height 140)
      ;; GUI環境のみ：フォントセットの設定（非GUIではエラーになるためガード）
      (when (display-graphic-p)
        (let ((fontspec (font-spec :family font-name)))
          (set-fontset-font t 'japanese-jisx0208 fontspec)
          (set-fontset-font t 'katakana-jisx0201 fontspec)
          (set-fontset-font t 'japanese-jisx0212 fontspec)
          (dolist (w '((basement . #x2000)
                       (kana . #x3000)
                       (cjk-misc . #x3000)
                       (kanji . #x4e00)))
            (set-fontset-font nil (car w) fontspec))))))

  ;; デーモン起動時（非GUI）でのエラー回避 + GUIフレーム作成時に自動適用
  (add-hook 'after-make-frame-functions
            (lambda (frame)
              (with-selected-frame frame
                (when (display-graphic-p)
                  (setup-fonts)))))
  ;; 初回からGUI環境の場合は即座に適用
  (when (display-graphic-p)
    (setup-fonts))

  ;;----
  ;; (when (display-graphic-p)
  ;;   ;; (setq language-environment 'Japanese)
  ;;
  ;;   ;; HackGen Console NF
  ;;   (add-to-list 'default-flame-alist '(font . "HackGen Console NF-14"))
  ;;   ;; デフォルトフォント（半角英数）の設定
  ;;   (set-face-attribute 'default nil :family "HackGen Console NF" :height 140)
  ;;
  ;;   (let ((fontspec (font-spec :family "HackGen Console NF")))
  ;;     (set-fontset-font t 'japanese-jisx0208 fontspec)
  ;;     (set-fontset-font t 'katakana-jisx0201 fontspec)
  ;;     (set-fontset-font t 'japanese-jisx0212 fontspec))
  ;;     ;; 日本語（漢字・ひらがな・カタカナ）のフォント設定
  ;;   (let ((ws '((basement . #x2000)
  ;;               (kana . #x3000)
  ;;               (cjk-misc . #x3000)
  ;;               (kanji . #x4e00))))
  ;;     (dolist (w ws)
  ;;       (set-fontset-font nil (car w) (font-spec :family "HackGen Console NF"))))
  ;; 
  ;;   ;; ;; Valign
  ;;   ;; (use-package valign
  ;;   ;;   :straight t
  ;;   ;;   :ensure t
  ;;   ;;   :config
  ;;   ;;   (global-valign-mode 1))
  ;;   )
#+end_src

** Line number

Show line numbers only programming language mode.

#+begin_src elisp :tangle emacs/init.el
  (add-hook 'prog-mode-hook 'display-line-numbers-mode)
#+end_src

** Recentf - keep track of recently opened files

Enable recentf-mode and keep large number of histories (10000).
The mode is called through [[*Consult][Consult]].

#+begin_src elisp :tangle emacs/init.el
  ;; recentf
  (recentf-mode 1)
  (setq recentf-max-saved-items 10000)
#+end_src

** Ibuffer

Prefer to use ibuffer instead of Emacs default one.

| Key bind | Description        |
|----------+--------------------|
| C-x C-b  | Show opened buffer |

#+begin_src elisp :tangle emacs/init.el
  ;; use ibuffer
  (global-set-key (kbd "C-x C-b") 'ibuffer)
#+end_src

** Backup file

Make backup file under the ~%HOME/.emacs.d/file-backup~ directory.

#+begin_src elisp :tangle emacs/init.el
  ;; Backup
  (setq make-backup-files t)
  ;; Backup directory
  (setq backup-directory-alist
        (cons (cons "\\.*$" (expand-file-name "~/.emacs.d/file-backup"))
              backup-directory-alist))
  (setq version-control t)                   ;; Multiple backups
  (setq kept-new-versions 5)                 ;; Number of new versions
  (setq kept-old-versions 5)                 ;; Number of the old versions
  (setq delete-old-versions t)               ;; Delete old version
#+end_src

** Magit

| Key bind | Description        |
|----------+--------------------|
| C-x g    | Enter magit status |

#+begin_src elisp :tangle emacs/init.el
  ;; Magit
  (use-package magit
    :straight t
    :config
    (global-set-key (kbd "C-x g") 'magit-status)
    (setq magit-auto-revert-mode nil))
#+end_src

** Consult

| Key bind | Description                             |
|----------+-----------------------------------------|
| C-c f    | Show recent files (recentf)             |
| C-c o    | Show outline (org-mode, rst-mode, etc.) |
| M-g g    | Goto line                               |

Consult is used with other packages.
Dependencies:
- Vertico
- Orderless
- Recentf
- Any of structured text mode (Org, Rst, Markdown, etc.)

#+begin_src elisp :tangle emacs/init.el
  ;; consult
  (use-package consult
    :straight t
    :config
    (global-set-key (kbd "C-c f") 'consult-recent-file)
    (global-set-key (kbd "C-c o") 'consult-outline)
    (global-set-key (kbd "M-g g") 'consult-goto-line))
#+end_src

** Vertico

Used by Consult.

#+begin_src elisp :tangle emacs/init.el
  ;; vertico
  (use-package vertico
    :straight t
    :config
    (vertico-mode))
#+end_src

** Orderless
Used by Consult and Vertico.
#+begin_src elisp :tangle emacs/init.el
  ;; Optionally use the `orderless' completion style.
  (use-package orderless
    :straight t
    :custom
    ;; Configure a custom style dispatcher (see the Consult wiki)
    ;; (orderless-style-dispatchers '(+orderless-consult-dispatch orderless-affix-dispatch))
    ;; (orderless-component-separator #'orderless-escapable-split-on-space)
    (completion-styles '(orderless basic))
    (completion-category-defaults nil)
    (completion-category-overrides '((file (styles partial-completion)))))
#+end_src

** Japanese Input Method - ddskk

Japanese input method for Emacs.
https://github.com/skk-dev/ddskk

| Key bind | Description         |
|----------+---------------------|
| C-\      | Toggle input method |

Initial setup:
Execute ~M-x skk-get~ to download Jisyo files under ~${HOME}/.emacs.d/skk-get-jisyo~ directory.

#+begin_src elisp :tangle emacs/init.el
  ;; ddskk
  (use-package ddskk
    :straight t
    :ensure t
    :config
    ;; Set ddskk as a default Japanese input method
    (setq default-input-method "japanese-skk")
    ;; Dictionary files
    (setq skk-jisyo '("~/skk-jisyo.utf-8" . utf-8))
    (setq dir-skk-get-jisyo (expand-file-name "~/.emacs.d/skk-get-jisyo/"))
    (setq skk-large-jisyo (concat dir-skk-get-jisyo "SKK-JISYO.L"))
    ;; Download SKK jisyo files if not exists
    (if (file-exists-p skk-large-jisyo)
        (message "SKK-JISYO.L exists")
      (skk-get dir-skk-get-jisyo))
    (setq skk-extra-jisyo-file-list
          (list (concat dir-skk-get-jisyo "SKK-JISYO.assoc")
                (concat dir-skk-get-jisyo "SKK-JISYO.edict")
                (concat dir-skk-get-jisyo "SKK-JISYO.geo")
                (concat dir-skk-get-jisyo "SKK-JISYO.itaiji")
                (concat dir-skk-get-jisyo "SKK-JISYO.jinmei")
                (concat dir-skk-get-jisyo "SKK-JISYO.JIS2")
                (cons (concat dir-skk-get-jisyo "SKK-JISYO.JIS3_4") 'euc-jis-2004)
                (concat dir-skk-get-jisyo "SKK-JISYO.law")
                (concat dir-skk-get-jisyo "SKK-JISYO.lisp")
                (concat dir-skk-get-jisyo "SKK-JISYO.mazegaki")
                ))
    ;; History file
    )
#+end_src
** Migemo

Enable migemo search only if cmigemo is installed.

Requirements:
- cmigemo (https://github.com/koron/cmigemo)

#+begin_src elisp :tangle emacs/init.el
  (defun get-cmigemo-path ()
    "Returns full path of `cmigemo` executable if it exists, otherwise nil."
    (let* ((cmigemo-path (executable-find "cmigemo"))
           (cmigemo-realpath (file-truename cmigemo-path)))
      ;; Return nil if not found
      (when cmigemo-path cmigemo-realpath)))
  (defun get-cmigemo-dict-path (cmigemo-path)
    "Returns full path of `cmigemo` dict directory if it exisits, otherwise nil."
    (if (string-match "^\\(.*\\)\\/bin\\/cmigemo$" cmigemo-path)
        (concat (match-string 1 cmigemo-path) "/share/migemo/utf-8/migemo-dict")
      ;; return nil if the pattern "/bin/cmigemo" doesn't match
      nil))
  (use-package migemo
    :straight t
    :if (when (get-cmigemo-path) t)  ;; install migemo only if the cmigemo installed
    :config
    (setq migemo-command (get-cmigemo-path))
    (setq migemo-dictionary (get-cmigemo-dict-path migemo-command))
    (migemo-init))
#+end_src
** Ghostel
https://github.com/dakra/ghostel
#+begin_src elisp :tangle emacs/init.el
  (use-package ghostel
    :straight t
    :ensure t
    :bind (("C-x m" . ghostel)
           :map project-prefix-map
                ("m" . ghostel-project)
                ("M" . ghostel-project-list-buffers))
    :config
    (add-to-list 'project-switch-commands '(ghostel-project "Ghostel") t)
    (add-to-list 'project-switch-commands '(ghostel-project-list-buffers "Ghostel buffers") t)
    (add-to-list 'ghostel-eval-cmds '("magit-status-setup-buffer" magit-status-setup-buffer))
  )
  (use-package ghostel-ime
    :hook (ghostel-mode . ghostel-ime-mode))
#+end_src
** Theme vscode-dark-plus
https://github.com/ianyepan/vscode-dark-plus-emacs-theme
#+begin_src elisp :tangle emacs/init.el
  (use-package vscode-dark-plus-theme
    :straight t
    :ensure t
    :config
    (load-theme 'vscode-dark-plus t)
    ;; Remove the border around the TODO word on org-mode files
    (setq vscode-dark-plus-box-org-todo nil)
    ;; Do not set different heights for some org faces
    (setq vscode-dark-plus-scale-org-faces nil)
    ;; Avoid inverting hl-todo face
    (setq vscode-dark-plus-invert-hl-todo nil)
    ;; Configure current line highlighting style (works best with Emacs 28 or newer)
    (setq vscode-dark-plus-render-line-highlight 'line)
  )
#+end_src

** Org-mode
Org-mode configuration

| Key bind | Description        |
|----------+--------------------|
| C-c l    | Store link         |
| C-c c    | Capture            |
| C-c a    | Agenda             |
| C-c b    | Switch Org buffers |

#+begin_src elisp :tangle emacs/init.el
  (use-package org
    :straight t
    :ensure t
    :config
    (global-set-key (kbd "C-c l") 'org-store-link)
    (global-set-key (kbd "C-c c") 'org-capture)
    (global-set-key (kbd "C-c a") 'org-agenda)
    (global-set-key (kbd "C-c b") 'org-switchb)
    (setq org-startup-indented t)
    (setq org-log-into-drawer t)
    (setq org-hide-emphasis-markers t)
    (setq org-hide-leading-stars t)
    (setq org-startup-indented t)
    (setq org-fontify-emphasized-text nil)
    (setq org-deadline-warning-days 14)
    ;; use relative path
    (setq org-link-file-path-type 'relative)
    ;; States keywords
    (setq org-todo-keywords
          '((sequence "TODO(t)" "NEXT(n)" "DOIN(i)" "WAIT(w)" "|" "DONE(d)" "|" "CNCL(c)")))
    ;; visual-line-mode
    ;; ;; Open files using external program
    ;; (setq org-file-apps
    ;;   '((auto-mode . emacs)
    ;; ("\\.mm\\'" . default)         ;; default
    ;; ("\\.x?html?\\'" . default)    ;; default
    ;; ("\\.pdf\\'" . "open \"%s\"")  ;; modified from ("\\.pdf\\'" . emacs)
    ;; ("\\.png\\'" . "open \"%s\"")  ;; added
    ;; ("\\.jpg\\'" . "open \"%s\"")  ;; added
    ;; ("\\.gif\\'" . "open \"%s\"")  ;; added
    ;; ("\\.svg\\'" . "open \"%s\"")  ;; added
    ;; ))

    ;; org-babel activate graphviz dot
    (org-babel-do-load-languages
     'org-babel-load-languages
     '((shell . t)
       (dot . t)
       (ruby . t)
       (python . t)
       ;; (gptel . t)
       ;;(blockdiag . t)
       ;;(mermaid . t)
       ))
    ;; ;; ob-shell might be different section
    ;; (require 'ob-shell)
    )
#+end_src

** Denote
https://protesilaos.com/emacs/denote
#+begin_src elisp :tangle emacs/init.el
  (use-package denote
    :straight t
    :ensure t
    :requires org
    :hook (dired-mode . denote-dired-mode)
    :bind
    (("C-c n n" . denote)
     ("C-c n r" . denote-rename-file)
     ("C-c n l" . denote-link)
     ("C-c n b" . denote-backlinks)
     ("C-c n d" . denote-dired)
     ("C-c n g" . denote-grep))
    :config
    (setq denote-directory (expand-file-name "~/Documents/denote"))
    ;; Org Agenda
    (setq org-agenda-files (append org-agenda-files (list denote-directory)))
    ;; Automatically rename Denote buffers when opening them
    (denote-rename-buffer-mode 1)
    (with-eval-after-load 'org-capture
      (add-to-list 'org-capture-templates
                   '("n" "New note (with Denote)" plain
                     (file denote-last-path)
                     #'denote-org-capture
                     :no-save t
                     :immediate-finish nil
                     :kill-buffer t
                     :jump-to-captured t)))
    )
#+end_src

** consult-denote
Integrate consult with denote

#+begin_src elisp :tangle emacs/init.el
  (use-package consult-denote
    :straight t
    :ensure t
    :requires denote
    :bind
    (("C-c n f" . consult-denote-find)
     ("C-c n g" . consult-denote-grep))
    :config
    (consult-denote-mode 1)
    )
#+end_src

** denote-org
Extra org integration
#+begin_src elisp :tangle emacs/init.el
  (use-package denote-org
    :straight t
    :ensure t
    :requires denote
    :commands
    (denote-org-link-to-heading
     denote-org-backlinks-for-heading
     denote-org-extract-org-subtree
     denote-org-convert-links-to-file-type
     denote-org-convert-links-to-denote-type
     denote-org-dblock-insert-files
     denote-org-dblock-insert-links
     denote-org-dblock-insert-backlinks
     denote-org-dblock-insert-missing-links
     denote-org-dblock-insert-files-as-headings)
  )
#+end_src

** denote-journal
#+begin_src elisp :tangle emacs/init.el
  (use-package denote-journal
    :straight t
    :ensure t
    ;; :requires denote
    ;; Bind those to some key for your convenience.
    :commands (denote-journal-new-entry
               denote-journal-new-or-existing-entry
               denote-journal-link-or-create-entry)
    :hook (calendar-mode . denote-journal-calendar-mode)
    :config
    ;; Use the "journal" subdirectory of the `denote-directory'.  Set this
    ;; to nil to use the `denote-directory' instead.
    (setq denote-journal-directory
          (expand-file-name "journal" denote-directory))
    ;; Add to agenda
    (setq org-agenda-files (append org-agenda-files (list denote-journal-directory)))
    ;; Default keyword for new journal entries. It can also be a list of
    ;; strings.
    (setq denote-journal-keyword "journal")
    ;; Read the doc string of `denote-journal-title-format'.
    ;; (setq denote-journal-title-format 'day-date-month-year)
    (setq denote-journal-title-format "%Y-%m-%d %A")
    ;; Set category
    (setq denote-templates
          '((journal . "#+CATEGORY: journal\n\n"))))
#+end_src

** denote-agenda
https://github.com/emacsmirror/denote-agenda
#+begin_src elisp :tangle emacs/init.el
  (use-package denote-agenda
    :straight t
    :ensure t
    :requires denote-journal
    :config
    ;; (setq denote-agenda-include-journal-limit 60)
    (denote-agenda-insinuate))
#+end_src

** Org-superstar
Decorate org-mode bullets.
https://github.com/integral-dw/org-superstar-mode

#+begin_src elisp :tangle emacs/init.el
  (use-package org-superstar
    :straight t
    :requires org
    :hook
    (org-mode-hook . (lambda() (org-superstar-mode 1)))
    )
#+end_src

** Markdown-mode
Markdown mode
#+begin_src elisp :tangle emacs/init.el
  ;; markdown-ts-mode
  (use-package markdown-ts-mode
    :ensure nil
    :mode ("\\.md\\'" "\\.mdx\\'" "\\.markdown\\'")
    :config
    (require 'markdown-ts-mode-x)
    ;; 見出し以外の主要なフェイスに default のフォント設定を一括適用
    ;; (mapc (lambda (face) (custom-set-faces `(,face ((t (:inherit default :family nil :height 1.0))))))
    ;;       '(markdown-table-face markdown-inline-code-face markdown-code-face markdown-pre-face markdown-markup-face))
    )
#+end_src

** Github Flavored Markdown Exporter for Org Mode

#+begin_src elisp :tangle emacs/init.el
  ;; ox-gfm
  (use-package ox-gfm
    :straight t)
#+end_src

** Programming Language
*** go-ts-mode

#+begin_src elisp :tangle emacs/init.el
  ;; go-ts-mode
  (use-package go-ts-mode
    :ensure nil
    :mode "\\.go\\'")
#+end_src

*** js-ts-mode
#+begin_src elisp :tangle emacs/init.el
  ;; js2-mode
  (use-package js-ts-mode
    :ensure nil
    :mode ("\\.js\\'")
    :config
    (setq js-indent-level 2)
    (setq js-expr-indent-offset 2))
#+end_src

*** json-ts-mode
#+begin_src elisp :tangle emacs/init.el
  ;; json-mode
  (use-package json-ts-mode
    :ensure nil
    :mode ("\\.json\\'" "\\.jsonl\\'")
    :config
    (setq js-indent-level 2)
    (setq js-expr-indent-offset 2))
#+end_src

*** nix-mode
Nix mode
https://github.com/NixOS/nix-mode
#+begin_src elisp :tangle emacs/init.el
  ;; nix-mode
  (use-package nix-mode
    :straight t
    :mode "\\.nix\\'")
#+end_src

*** powershell
PoserShell mode
#+begin_src elisp :tangle emacs/init.el
  ;; powershell mode
  (use-package powershell
    :straight t
    :mode (("\\.ps\\'" . powershell-mode)
           ("\\.ps1\\'" . powershell-mode)))
#+end_src

*** typescript-ts-mode
Typescript mode
#+begin_src elisp :tangle emacs/init.el
  ;; typescript-ts-mode
  (use-package typescript-ts-mode
    :ensure nil
    :mode (("\\.tsx\\'" . tsx-ts-mode)
           ("\\.ts\\'" . tsx-ts-mode))
    :config
    (setq typescript-ts-mode-indent-offset 2))
#+end_src

*** typst-ts-mode

#+begin_src elisp :tangle emacs/init.el
  ;; typst-ts-mode
  (use-package typst-ts-mode
    :ensure nil
    :mode "\\.typ\\'")
#+end_src

*** yaml-ts-mode
#+begin_src elisp :tangle emacs/init.el
  ;; yaml-ts-mode
  (use-package yaml-ts-mode
    :ensure nil
    :mode (("\\.yml\\'" . yaml-ts-mode)
           ("\\.yaml\\'" . yaml-ts-mode)))
#+end_src

** Treesit

Treesit auto-install-grammar is introduced from Emacs 31.
#+begin_src elisp :tangle emacs/init.el
  ;; One line replaces thirty minutes of manual setup
  (setq treesit-auto-install-grammar 'always)
  (setq completion-eager-display 'auto
        completion-eager-update 'auto)
#+end_src

** ob-async
#+begin_src elisp :tangle emacs/init.el
  (use-package ob-async
    :straight t)
#+end_src

** ollama-buddy.el
https://github.com/captainflasmr/ollama-buddy

The packate ~ollama-buddy.el~ requires Ollama installation.

Requirement:
Ollama: https://ollama.com

#+begin_src elisp :tangle emacs/init.el
  (use-package ollama-buddy
    :straight t
    :ensure t
    :bind
    ;; ("C-c o" . ollama-buddy-menu)  ;; conflicts with consult-outline
    (("C-c O" . ollama-buddy-transient-menu))
    :config
    )
#+end_src

** gptel

https://github.com/karthink/gptel

#+begin_src elisp :tangle emacs/init.el
  (use-package gptel
    :straight t
    :ensure t
    :config
    (setq gptel-default-mode 'markdown-ts-mode)
    (gptel-make-openai "llama-cpp"                      ;;  Any name
      :stream t                                         ;; Stream responses
      :protocol "http"                                  ;; Llama.cpp server doesn't use TLS
      :host "localhost:9931"                            ;; Llama.cpp server location
      :models '("ggml-org/gemma-4-E4B-it-GGUF:Q8_0")    ;; Any names, doesn't matter for Llama
      :key ""                                           ;; No authentication
      )
    (setq gptel-model 'ggml-org/gemma-4-E4B-it-GGUF:Q8_0)
    )
#+end_src

** gptel-agent

https://github.com/karthink/gptel-agent

#+begin_src elisp :tangle emacs/init.el
  (use-package gptel-agent
    :straight t
    :config
    (gptel-agent-update)
  )
#+end_src

** ob-gptel

#+begin_src elisp :tangle emacs/init.el
  (use-package ob-gptel
    :straight (ob-gptel
                :type git
                :host github
                :repo "jwiegley/ob-gptel")
    :config
    (add-to-list 'org-babel-load-languages '(gptel . t))
    (defun ob-gptel-setup-completions ()
      (add-hook 'completion-at-point-functions
                'ob-gptel-capf nil t))
    :hook (org-mode . ob-gptel-setup-completions))
#+end_src

** Custom file

Customized configuration differs per enviornment.
This settings must be loaded at the last.

#+begin_src elisp :tangle emacs/init.el
  ;; Custom file
  (setq custom-file "~/.emacs.d/emacs-custom.el")
  (when (file-exists-p custom-file)
    (load custom-file))
#+end_src
