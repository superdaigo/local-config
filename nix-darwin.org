[Work in progress]
* Deploy
#+begin_src elisp :results none
  ;; local variables
  (setq-local config-files (list "flake.nix" "configuration.nix"))
  (setq-local dst-dir (expand-file-name "/etc/nix-darwin"))
  (setq-local tmp-dir (file-name-base buffer-file-name))
  (setq-local backup-prefix (format-time-string "%Y-%m-%d_%H%M%S_"))
  ;; Create directories
  (dolist (dir-name (list dst-dir tmp-dir))
    (make-directory dir-name t))
  ;; Generate file
  (org-babel-tangle-file buffer-file-name)
  ;; Copy current files and deploy
  (dolist (config-file config-files)
    (let ((tmp-file (file-name-concat tmp-dir config-file))
          (bak-file (file-name-concat tmp-dir (concat backup-prefix config-file)))
          (dst-file (file-name-concat dst-dir config-file)))
      ;; Take backup
      (if (file-exists-p dst-file)
          (copy-file dst-file bak-file)
        nil)
      ;; Save new file
      (copy-file tmp-file dst-file t)))
#+end_src
* Commands
** Init
#+begin_src bash
  sudo nix run nix-darwin/nix-darwin-26.05#darwin-rebuild --extra-experimental-features 'flakes nix-command' -- switch
#+end_src
** Rebuild
#+begin_src bash
  sudo darwin-rebuild build --flake /etc/nix-darwin
#+end_src
** Switch to the new build
#+begin_src bash
  sudo darwin-rebuild switch --flake /etc/nix-darwin
#+end_src
** List generations
#+begin_src bash
  sudo darwin-rebuild --list-generations
#+end_src
** Switch to a specific generation
#+begin_src bash
  sudo darwin-rebuild --switch-generation N
#+end_src
** Delete Cache and previous builds
Delete generations older than 14days
#+begin_src bash
  sudo nix-collect-garbage --delete-older-than 14d
#+end_src

Delete all generations
#+begin_src bash
  sudo nix-collect-garbage -d
#+end_src
** Upgrade
Check the README.md on GitHub.
https://github.com/nix-darwin/nix-darwin#getting-started
*** 25.11 -> 26.05
Some modifications are requried and added a `./configuration.nix` file.
#+begin_src bash
  sudo nix run nix-darwin/nix-darwin-26.05#darwin-rebuild -- switch
#+end_src

* Config - flake.nix

#+begin_src nix :tangle nix-darwin/flake.nix
  # -*- coding: utf-8 -*-
  # flake.nix - nix-darwin configuration file
  # Generated by nix-darwin.org file using org-babel-tangle-file
  {
    description = "Simple nix-darwin system flake";

    inputs = {
      nixpkgs.url = "github:NixOS/nixpkgs/nixpkgs-26.05-darwin";
      nix-darwin.url = "github:nix-darwin/nix-darwin/nix-darwin-26.05";
      nix-darwin.inputs.nixpkgs.follows = "nixpkgs";
    };

    outputs = inputs@{ self, nix-darwin, nixpkgs }:
    {
      # Build darwin flake using:
      # $ sudo darwin-rebuild build --flake /etc/nix-darwin#simple
      darwinConfigurations."simple" = nix-darwin.lib.darwinSystem {
        modules = [ ./configuration.nix ];
      };
      # Per-device configuration
      # $ sudo darwin-rebuild build --flake /etc/nix-darwin
      # Host: MBA-YABUPGG
      darwinConfigurations."MBA-YABUPGG" = nix-darwin.lib.darwinSystem {
        modules = [ ./configuration.nix ];
        specialArgs = { inherit inputs; };
      };
      # Host: PC-00096
      darwinConfigurations."PC-00096" = nix-darwin.lib.darwinSystem {
        modules = [ ./configuration.nix ];
        specialArgs = { inherit inputs; };
      };
      # Add if any host
    };
  }
#+end_src
* Config - configuration.nix

Configuration for Apple sillicon mac

#+begin_src nix :tangle nix-darwin/configuration.nix
  # -*- coding: utf-8 -*-
  # configuration.nix - nix-darwin configuration file
  { pkgs, inputs, ... }:

  {
    # List packages installed in system profile. To search by name, run:
    # $ nix-env -qaP | grep wget
    environment.systemPackages = with pkgs; [
      awscli2
      bun
      cmigemo
      curl
      direnv
      ffmpeg-full
      git
      gh
      gnused
      go
      google-cloud-sdk
      graphviz
      httpie
      imagemagick
      jq
      keka
      marp-cli
      nmap
      nodejs
      osv-scanner
      ripgrep
      podman
      podman-compose
      sqlite
      terraform
      tmux
      typst
      uv
      wireshark
      yazi
      yt-dlp
      zsh
    ];

    # Necessary for using flakes on this system.
    nix.settings.experimental-features = "nix-command flakes";

    # Enable alternative shell support in nix-darwin.
    # programs.fish.enable = true;

    # Set Git commit hash for darwin-version.
    system.configurationRevision = inputs.self.rev or inputs.self.dirtyRev or null;

    # Used for backwards compatibility, please read the changelog before changing.
    # $ darwin-rebuild changelog
    system.stateVersion = 7;

    # The platform the configuration will be used on.
    nixpkgs.hostPlatform = "aarch64-darwin";

    # Allow specific Unfree package
    nixpkgs.config.allowUnfree = true;
  }
#+end_src
